{% extends 'base.html.twig' %}
{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
    <a href="{{ path('town_listing') }}" class="btn btn-secondary" title="cancel">Annuler</a>
{% endblock %}